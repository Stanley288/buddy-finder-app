type Query {
  # Find user by id
  user(id: String!): User

  # Find user by email
  userByEmail(email: String!): User
}

type Mutation {
  # Create user
  createUser(input: SignupInput!): User
}

schema {
  query: Query
  mutation: Mutation
}
